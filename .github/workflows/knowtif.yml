# Knowtif - GitHub Notifications
# Generated by: npx knowtif install

name: Knowtif

on:
  push:
    branches: [ "main", "master" ]
  workflow_run:
    workflows: ["*"]
    types: [completed]
  deployment_status:
  pull_request:
    types: [opened, closed, merged]
  release:
    types: [published]


jobs:
  notify:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.name != 'Knowtif' || github.event_name != 'workflow_run' }}
    steps:
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npx knowtif@latest action
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DISCORD_WEBHOOK: "https://discord.com/api/webhooks/1449997330391765118/V7hPNne0CO0yzbYWpDwowxQIa7rtj_IYd_N7WJBOzcfjZ6dnHiiuLgGf5zeocHrifU6p"
          NTFY_TOPIC: "knowtif-knowtif-cb3e6a5c"
          NTFY_SERVER: "https://ntfy.sh"
